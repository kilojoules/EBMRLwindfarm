\documentclass{article}

% NeurIPS 2026 style
\usepackage[final]{neurips_2026}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{subcaption}
\usepackage{xcolor}

\newcommand{\ie}{\textit{i.e.}}
\newcommand{\eg}{\textit{e.g.}}
\newcommand{\cf}{\textit{cf.}}
\newcommand{\etal}{\textit{et al.}}

\title{Adaptive Budget Scheduling for Constrained RL Deployment \\ via Energy Composition}

\author{
  Julian Quick \\
  DTU Wind Energy \\
  Technical University of Denmark \\
  \texttt{juqu@dtu.dk}
  \And
  Marcus Binder Nilsen \\
  DTU Wind Energy \\
  Technical University of Denmark \\
}

\begin{document}

\maketitle

\begin{abstract}
Deploying reinforcement learning policies in the real world often requires cumulative operational constraints unknown at training time---maintenance budgets, emission limits, duty-cycle restrictions.
Standard constrained MDP approaches require full retraining for each constraint specification.
We propose a closed-form, time-varying penalty weight motivated by a budget-balanced sequential allocation problem: the urgency ratio $u$ (remaining budget fraction over remaining time fraction) determines a multiplicative weight $w(u) = \exp(\eta(1/u - 1))$ that adapts penalty strength to budget state.
A single risk-aversion parameter $\eta$ interpolates between uniform allocation ($\eta=0$) and aggressive concentration.
The schedule composes post-hoc with pre-trained energy-based policies via per-agent energy addition---requiring zero retraining.
Across two domains---wind farm yaw control and MuJoCo locomotion with velocity budgets---the method satisfies cumulative budgets while retaining 77--96\% of unconstrained reward, outperforming constant-penalty baselines by 7--20 percentage points in budget utilization.
A single pre-trained policy supports arbitrary budget levels, per-agent heterogeneous constraints, and instant adaptation---all without retraining.
\end{abstract}

%----------------------------------------------------------------------
\section{Introduction}
\label{sec:intro}
%----------------------------------------------------------------------

Consider a wind farm operator who trains a reinforcement learning (RL) controller to maximize power production.
Six months after deployment, a bearing inspection reveals that one turbine needs restricted yaw travel: it may operate at negative yaw angles for at most 15 hours per year to limit structural fatigue.
Retraining the controller is expensive and disruptive.
The operator needs a way to impose this new cumulative constraint on the \emph{existing} policy, instantly and without retraining.

This scenario is ubiquitous in deployed RL systems.
Cumulative operational constraints arise whenever an agent must limit the total time spent in a high-reward but costly regime:
wind turbines with load budgets~\citep{dijk2025load},
robots with joint-wear maintenance cycles,
grid batteries with cycle-life limits,
and advertisers with daily spending caps~\citep{balseiro2023budget}.
Unlike instantaneous constraints (avoid obstacles, stay upright), these are \emph{lifetime budgets}: the agent must decide \emph{when} to spend its limited capacity for risky actions, allocating strategically across time.

This budget allocation problem shares a conceptual structure with optimal trade execution in quantitative finance~\citep{almgren2001optimal}: a trader must liquidate a position over a time horizon, deciding when to trade aggressively versus passively.
While the full financial model involves market impact dynamics with no analog in our setting, a key insight transfers: the rate of resource consumption should depend on \emph{remaining inventory relative to remaining time}---the urgency ratio.

Existing approaches to constrained RL fall into two categories, neither of which solves this problem:
(1)~\textbf{Constrained MDP methods}~\citep{altman1999constrained, stooke2020responsive, sootla2022saute, chen2025predictive} learn a Lagrangian dual variable during training, requiring full retraining for each new constraint specification.
(2)~\textbf{Post-hoc constraint composition}~\citep{urain2023composable, zhang2025constrained, xiao2023safediffuser} adds constraints at inference time via energy composition or classifier guidance, but handles only \emph{instantaneous} constraints (barrier functions, geometric limits)---not cumulative budgets that span an episode.

We fill this gap.
Our contributions are:
\begin{enumerate}
    \item A \textbf{closed-form time-varying penalty weight} derived from budget-balanced sequential allocation under Boltzmann policy response (Section~\ref{sec:theory}). The optimal weight $w^*(u) = 1/u$ depends only on the urgency ratio $u = (\text{budget remaining} / \text{budget total}) / (\text{time remaining} / \text{horizon})$.
    \item A \textbf{practical schedule} $w(u) = \exp(\eta(1/u - 1))$ that approximates the optimum to first order, recovers uniform allocation at $\eta=0$, and provides tunable risk aversion via a single scalar parameter (Section~\ref{sec:method}).
    \item \textbf{Post-hoc composition} with pre-trained energy-based transformer (EBT) actors via per-agent energy addition, enabling zero-retraining constraint enforcement with heterogeneous per-agent budgets (Section~\ref{sec:composition}).
    \item \textbf{Empirical validation} across two domains---wind farm yaw control and MuJoCo locomotion---showing budget satisfaction, reward retention, and superiority over constant-penalty baselines (Section~\ref{sec:results}).
\end{enumerate}

%----------------------------------------------------------------------
\section{Background}
\label{sec:background}
%----------------------------------------------------------------------

\subsection{Constrained MDPs and Lagrangian Relaxation}

A Constrained MDP (CMDP)~\citep{altman1999constrained} augments the standard MDP objective with cost constraints:
\begin{equation}
    \max_\pi \; \mathbb{E}_\pi\!\left[\sum_{t=0}^{T-1} r(s_t, a_t)\right]
    \quad \text{s.t.} \quad
    \mathbb{E}_\pi\!\left[\sum_{t=0}^{T-1} c^i(s_t, a_t)\right] \leq B^i
    \quad \forall\, i
    \label{eq:cmdp}
\end{equation}
where $c^i$ is a per-agent cost indicator and $B^i$ is the lifetime budget for agent $i$.
The standard approach introduces a Lagrangian dual variable $\mu$:
\begin{equation}
    \mathcal{L}(\pi, \mu) = \mathbb{E}_\pi\!\left[\sum_t r(s_t, a_t)\right] - \mu \cdot \left(\mathbb{E}_\pi\!\left[\sum_t c(s_t, a_t)\right] - B\right)
\end{equation}
and alternates between policy optimization (primal) and multiplier updates via dual gradient ascent: $\mu_{k+1} = \max(0,\, \mu_k + \alpha(\hat{C}_k - B))$~\citep{stooke2020responsive}.
This requires retraining the policy whenever $B$ changes.

\subsection{Energy-Based RL and Post-Hoc Composition}

Energy-Based Transformer (EBT) actors~\citep{gladstone2025ebt} generate actions by gradient descent on a learned energy landscape.
Given per-agent embeddings $\mathbf{e}^i$ from a transformer encoder, an energy head $E_\phi^i(\mathbf{e}^i, a^i)$ assigns scalar energy to candidate actions.
Actions are optimized via:
\begin{equation}
    a^{(k+1)} = \mathrm{clip}\!\left(a^{(k)} - \eta_{\mathrm{lr}} \nabla_a E_{\mathrm{total}}(a^{(k)}),\; -1,\; 1\right)
\end{equation}
The key property enabling post-hoc constraints: external energy terms compose \emph{per-agent, before aggregation}:
\begin{equation}
    E_{\mathrm{total}}(a) = \frac{1}{N}\sum_{i=1}^{N}\!\left[E_\phi^i(\mathbf{e}^i, a^i) + \lambda_{\mathrm{gs}} \cdot E_{\mathrm{constraint}}^i(a^i)\right]
    \label{eq:composition}
\end{equation}
This enables adding new constraints at inference time without modifying $E_\phi$.
Prior work composes instantaneous constraints (\eg, yaw limits, obstacle avoidance)~\citep{urain2023composable, zhang2025constrained}.
We extend this to \emph{cumulative budget constraints} via a time-varying energy weight.

%----------------------------------------------------------------------
\section{Optimal Budget Scheduling}
\label{sec:theory}
%----------------------------------------------------------------------

We motivate the penalty weight schedule from a sequential budget allocation problem.
The derivation identifies the urgency ratio as the natural state variable and constrains the design space; the specific functional form is a principled choice justified by its analytical properties rather than exact optimality for any particular policy class.

\textbf{Setup.}
An agent must allocate $B$ ``risky'' actions over $T$ time steps.
At each step $t$, the agent observes conditions and decides whether to spend one unit of budget.
Spending yields a time-dependent benefit; the goal is to maximize total benefit subject to the budget constraint.

\textbf{Budget tracking.}
Let $C_t^i = \sum_{s=0}^{t-1} c^i(a_s)$ denote the cumulative cost for agent $i$ at time $t$, where $c^i(a) = \mathbf{1}[a^i \in \mathcal{A}_{\mathrm{risky}}]$ is the cost indicator (\eg, $\mathbf{1}[\text{yaw} < 0]$ for wind farms, $\mathbf{1}[\text{velocity} > v_{\mathrm{thresh}}]$ for locomotion).
Define the \emph{urgency ratio}:
\begin{equation}
    u_t^i = \frac{\beta_t^i}{\tau_t}, \qquad
    \beta_t^i = \frac{B^i - C_t^i}{B^i}, \qquad
    \tau_t = \frac{T - t}{T}
    \label{eq:urgency}
\end{equation}
where $\beta_t^i$ is the remaining budget fraction and $\tau_t$ is the remaining time fraction.
When $u > 1$, the agent has spent less than the uniform (TWAP) rate; when $u < 1$, it has overspent.

\begin{proposition}[Motivating analysis under idealized Boltzmann response]
\label{prop:optimal}
Consider an idealized policy that responds to penalty weight $\lambda$ via a Boltzmann model:
$P(\text{risky} \mid \lambda) = P_0 \exp(-\alpha \lambda)$.
Budget balance over the remaining horizon requires $\tau \cdot P_0 \exp(-\alpha\lambda) = b$,
where $b$ is the remaining budget.
The multiplicative weight relative to the TWAP baseline is:
\begin{equation}
    w^*(u) = \frac{1}{u}
    \label{eq:optimal}
\end{equation}
\end{proposition}

\begin{proof}
At the TWAP point ($u=1$), budget balance gives $\lambda_0 = \frac{1}{\alpha}\ln\!\frac{TP_0}{B}$.
For general budget state: $\lambda = \frac{1}{\alpha}\ln\!\frac{\tau P_0}{b}$.
The multiplicative weight $w = \exp(\alpha(\lambda - \lambda_0)) = \frac{b/B}{\tau/T}^{-1} = 1/u$.
\end{proof}

\textbf{Practical schedule.}
Since $1/u \to \infty$ as $u \to 0$ (budget depleted), we propose the approximation:
\begin{equation}
    w(u) = \exp\!\left(\eta \cdot \left(\frac{1}{u} - 1\right)\right)
    \label{eq:practical}
\end{equation}

\begin{corollary}[First-order approximation]
Taylor expansion around $u=1$: $w(u) = 1 + \eta(1/u - 1) + O((1/u - 1)^2)$, matching $1/u = 1 + (1/u - 1)$ to first order when $\eta = 1$.
\end{corollary}

The parameter $\eta$ controls risk aversion:
$\eta = 0$ recovers the constant-penalty TWAP baseline (no adaptation);
$\eta = 1$ matches the Boltzmann optimum to first order;
$\eta > 1$ provides a more conservative safety margin.

\textbf{Hard wall backstop.}
A steep exponential barrier activates when budget drops below 5\%:
\begin{equation}
    \lambda_{\mathrm{wall}}^i(t) = \exp\!\left(k \cdot \mathrm{ReLU}\!\left(1 - \frac{\beta_t^i}{0.05}\right)\right)
\end{equation}
The combined weight is $\lambda^i(t) = \min(w(u_t^i) \cdot \lambda_{\mathrm{wall}}^i(t),\; \lambda_{\max})$, clamped at $\lambda_{\max} = 10^6$.

\textbf{Approximation quality and defense in depth.}
The practical schedule $\exp(\eta(1/u-1))$ matches $u^{-\eta}$ within 5\% for $u \in [0.7, 1.3]$ but diverges for $u < 0.3$ (Figure~\ref{fig:approx}).
This is by design: the method operates in three regimes.
In the \emph{normal} regime ($u \in [0.5, 1.5]$), the smooth schedule paces spending.
In the \emph{depletion} regime ($u < 0.05$), the hard wall backstop provides a steep barrier.
Between them, both the smooth schedule and the wall contribute.
The hard guard ($C^i < B^i$) provides an absolute guarantee.
Ablation results (Table~\ref{tab:hard_guard}) show that the smooth schedule alone is sufficient in practice---the hard guard does not change results.

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_approx_quality.pdf}
    \caption{(a)~Schedule comparison: $\exp(\eta(1/u-1))$ (solid) vs.\ $u^{-\eta}$ (dashed) across the full urgency range. Red region: hard wall backstop active ($u < 0.05$). (b)~Relative approximation error. The schedules agree within 10\% for $u > 0.5$ (the regime where smooth pacing operates).}
    \label{fig:approx}
\end{figure}

\textbf{Penalty landscape.}
Figure~\ref{fig:contours} visualizes $\lambda(\beta, \tau)$ as a function of remaining budget fraction $\beta$ and remaining time fraction $\tau$ for three risk-aversion values.
The TWAP diagonal ($\beta = \tau$, where $u=1$) separates the surplus region ($\lambda < 1$, agent may spend freely) from the deficit region ($\lambda > 1$, agent conserves).
At $\eta = 0.5$, gradients are gentle---the schedule tolerates moderate budget imbalance.
At $\eta = 5$, the penalty rises steeply near the diagonal, aggressively penalizing any deviation from the TWAP trajectory.
The hard wall (red band at $\beta < 0.05$) provides a safety backstop independent of $\eta$.

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_lambda_contours.pdf}
    \caption{Penalty weight $\lambda(\beta, \tau)$ across the budget--time state space for three risk-aversion values $\eta$. The TWAP diagonal (white dashed, $u = \beta/\tau = 1$) separates surplus (blue, $\lambda < 1$) from deficit (red, $\lambda > 1$). Contour lines show penalty magnitudes. Higher $\eta$ produces steeper gradients near the diagonal, concentrating budget spending into favorable conditions.}
    \label{fig:contours}
\end{figure}

\textbf{Note on the Boltzmann assumption.}
Proposition~\ref{prop:optimal} assumes a Boltzmann response, which is approximate for EBT actors optimized by gradient descent and does not hold for standard MLP policies.
The derivation serves to identify the urgency ratio $u$ as the natural state variable and to constrain the schedule to be monotone decreasing in $u$ with TWAP recovery at $\eta=0$.
The specific functional form $\exp(\eta(1/u-1))$ is a parametric choice satisfying these properties; it is not claimed to be optimal for any particular policy architecture.

%----------------------------------------------------------------------
\section{Method}
\label{sec:method}
%----------------------------------------------------------------------

The method has three components that compose independently:
(1)~the \emph{budget schedule} $\lambda(t)$ from Section~\ref{sec:theory}, which determines \emph{how strictly} to enforce the constraint at each moment;
(2)~the \emph{cost function} $c(s, a)$, which defines \emph{what} is costly;
and (3)~the \emph{application mechanism}, which determines \emph{how} the penalty modifies actions.
Any cost function composes with any mechanism and the same $\lambda(t)$ schedule.

\subsection{Cost Functions: From Binary Indicators to Nonlinear Surrogates}
\label{sec:cost}

The simplest cost is a binary indicator: $c(a) = \mathbf{1}[a \in \mathcal{A}_{\mathrm{risky}}]$ (\eg, $\mathbf{1}[\text{yaw} < 0]$).
This counts timesteps of risky behavior but ignores severity---10$^\circ$ of negative yaw is penalized the same as 30$^\circ$.

For real applications, a \emph{nonlinear surrogate} captures the physics:
\begin{equation}
    c(s, a) = \left(\frac{|a|}{\bar{a}}\right)^\beta \cdot \left(\frac{v(s)}{v_{\mathrm{ref}}}\right)^\gamma
    \label{eq:nonlinear_cost}
\end{equation}
where $v(s)$ is a state-dependent amplifier (\eg, wind speed), $\beta > 1$ captures superlinear damage from large actions, and $\gamma > 1$ captures environmental amplification.
For wind turbine fatigue, $\beta = 2$ (quadratic yaw-fatigue) and $\gamma = 3$ (cubic wind-speed amplification) are physically motivated~\citep{dijk2025load}.

The cost function must be differentiable for gradient-based application mechanisms (Section~\ref{sec:gradient}).
All cost functions compose with the same $\lambda(t)$ schedule; swapping the cost requires no retraining.

\subsection{Application Mechanism 1: Energy Composition (EBT Actors)}
\label{sec:composition}

For Energy-Based Transformer actors, the cost composes directly with the policy energy:
\begin{equation}
    E_{\mathrm{total}}(a, t) = \frac{1}{N}\sum_{i=1}^{N}\!\left[E_\phi^i(\mathbf{e}_t^i, a^i) + \lambda_{\mathrm{gs}} \cdot \lambda^i(t) \cdot P(c(s, a^i))\right]
    \label{eq:composition}
\end{equation}
where $P(x) = \exp(k \cdot \mathrm{ReLU}(x)) - 1$ is an exponential penalty and $\lambda_{\mathrm{gs}}$ is the guidance scale.
Gradient descent on $E_{\mathrm{total}}$ simultaneously optimizes reward and respects the budget.
This is the native mechanism for EBT actors and produces the strongest results.

\subsection{Application Mechanism 2: Gradient-Based Action Correction (Any Actor)}
\label{sec:gradient}

For standard MLP or Gaussian actors that do not support energy composition, we correct the actor's output using the cost function's gradient:
\begin{equation}
    a_{\mathrm{corrected}} = a_{\mathrm{actor}} - \lambda_{\mathrm{gs}} \cdot \lambda(t) \cdot \alpha \cdot \nabla_a\, c(s, a_{\mathrm{actor}})
    \label{eq:gradient_correction}
\end{equation}
iterated for $K$ steps (typically $K = 3$--$5$), where $\alpha$ is a step size.
This is mathematically equivalent to one step of gradient descent on the composed energy $E_\phi + \lambda(t) \cdot c$, but applied to the frozen actor's output rather than optimized from scratch.

The gradient naturally handles nonlinear and spatial costs that action scaling cannot.
For velocity constraints, $\nabla_a c$ points toward lower action magnitude (equivalent to action scaling).
For spatial constraints (\eg, hazard proximity), $\nabla_a c$ points \emph{away from hazards}---redirecting the agent's trajectory, not just slowing it down.

\subsection{Algorithm}

\begin{algorithm}[t]
\caption{Post-Hoc Budget-Constrained Inference}
\label{alg:budget}
\begin{algorithmic}[1]
\REQUIRE Trained actor, budget $B^i$ per agent, horizon $T$, risk aversion $\eta$, cost function $c(s, a)$
\STATE Initialize $C^i \leftarrow 0$ for all agents $i$
\FOR{$t = 0, \ldots, T-1$}
    \STATE Observe state $s_t$
    \STATE Compute urgency $u_t^i = \frac{(B^i - C^i)/B^i}{(T-t)/T}$ and $\lambda^i(t) = \min(\exp(\eta(1/u_t^i - 1)) \cdot \lambda_{\mathrm{wall}}^i,\; \lambda_{\max})$
    \STATE Generate action: \hfill\COMMENT{choose one mechanism}
    \STATE \quad \textit{EBT:} gradient descent on $E_\phi + \lambda_{\mathrm{gs}} \cdot \lambda^i(t) \cdot P(c(s, a))$
    \STATE \quad \textit{MLP:} $a \leftarrow a_{\mathrm{actor}} - \lambda_{\mathrm{gs}} \cdot \lambda^i(t) \cdot \alpha \cdot \nabla_a c(s, a)$ for $K$ steps
    \STATE Execute $a_t$, observe $r_t$, compute $c_t = c(s_t, a_t)$
    \STATE Update $C^i \leftarrow C^i + c_t^i$ \hfill\COMMENT{budget tracking}
\ENDFOR
\end{algorithmic}
\end{algorithm}

The computational overhead is small: 0.04~ms per step for the $\lambda$ computation (10.8\% on HalfCheetah).
Gradient correction adds $K$ backward passes through the cost function, which is typically much smaller than the policy network.

\subsection{Heterogeneous Per-Agent Budgets}

The framework supports per-agent budgets $B^i$ by computing $u_t^i$ independently.
This enables scenarios where agents have different structural health (\eg, a turbine with a recently replaced bearing tolerates more aggressive yaw).

\subsection{Connection to CMDP Lagrangian Duality}

The time-varying $\lambda^i(t)$ serves a similar role to the Lagrangian dual variable $\mu$ in~\eqref{eq:cmdp}: it penalizes costly actions proportionally to budget scarcity.
The key difference: $\mu$ is learned via dual gradient ascent (requiring retraining); $\lambda^i(t)$ is computed in closed form from the current budget state, enabling instant adaptation to new specifications.

%----------------------------------------------------------------------
\section{Experimental Setup}
\label{sec:setup}
%----------------------------------------------------------------------

We evaluate the budget scheduling mechanism across two domains with different risky-action semantics.

\subsection{Domain 1: Wind Farm Yaw Control}

\textbf{Environment.}
WindGym~\citep{windgym} with PyWake backend.
We test on two layouts: a 3-turbine row and a 9-turbine $3\!\times\!3$ grid.
Wind speed sampled from 10--14~m/s, wind direction from 225--315$^\circ$, providing natural temporal variation in wake conditions.

\textbf{Risky action.}
Negative yaw angles ($a^i < 0$) steer the rotor against the conventional direction, deflecting wakes to increase downstream power but imposing structural loads on the yawed turbine~\citep{dijk2025load}.

\textbf{Budget.}
$B$ = total timesteps at negative yaw per turbine per episode.

\textbf{Agent.}
Energy-Based Transformer (EBT) actor with Soft Actor-Critic (SAC), trained unconstrained for 100k steps.
The budget surrogate composes post-hoc via per-turbine energy addition at inference.

\subsection{Domain 2: MuJoCo Locomotion}

\textbf{Environment.}
HalfCheetah-v5~\citep{todorov2012mujoco} with a time-varying reward multiplier $m(t) = 1 + A\sin(2\pi t / T_p)$ ($A=0.5$, $T_p=200$) that creates temporal structure: forward progress is worth more during high-multiplier phases.

\textbf{Risky action.}
Exceeding a velocity threshold (set at the 50th percentile of unconstrained velocity).

\textbf{Budget.}
$B$ = total timesteps above the velocity threshold.

\textbf{Agent.}
SAC with Gaussian actor (standard MLP), trained unconstrained for 100k steps.
Since MLP actors do not support energy composition, the budget penalty is applied as an action scaling factor: $a \leftarrow a / (1 + 0.1 \cdot \lambda(t) \cdot p(a))$, where $p(a)$ is the base penalty.
The \emph{schedule} $\lambda(t)$ is identical to the EBT case; only the \emph{application mechanism} differs.
This demonstrates that the budget schedule is architecture-agnostic, even though energy composition (Section~\ref{sec:composition}) is specific to EBT actors.

\subsection{Baselines}

\begin{itemize}
    \item \textbf{Unconstrained}: no budget constraint (upper bound on reward).
    \item \textbf{Hard clip}: risky action banned entirely (lower bound on reward with budget).
    \item \textbf{Constant penalty} ($\eta = 0$): fixed penalty weight, no adaptation to budget state. Equivalent to a static Lagrangian multiplier.
    \item \textbf{AC schedule} ($\eta > 0$): our time-varying weight with risk aversion $\eta \in \{0.5, 1, 2, 5\}$.
\end{itemize}

\subsection{Ablations}

\begin{enumerate}
    \item \textbf{Hard guard vs.\ AC schedule}: Does the schedule alone prevent budget violations, or is the hard guard ($C^i < B^i$) necessary?
    \item \textbf{$1/u$ vs.\ $\exp(\eta(1/u-1))$}: Theoretically optimal schedule vs.\ practical approximation.
    \item \textbf{Budget flexibility}: Same policy at budget levels 10\%, 15\%, 25\%, 50\%, 75\%.
    \item \textbf{Boltzmann response validation}: Is the spending rate approximately exponential in the penalty weight?
\end{enumerate}

%----------------------------------------------------------------------
\section{Results}
\label{sec:results}
%----------------------------------------------------------------------

We present results across both domains, focusing on budget satisfaction, reward retention, and the physical understanding of when the agent chooses to incur damage.
Figure~\ref{fig:mechanism} illustrates how the penalty weight $\lambda(t)$ adapts during one HalfCheetah episode, and Figure~\ref{fig:physical} shows when the agent decides to spend its budget.

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fig1_mechanism.pdf}
    \caption{Budget scheduling mechanism over one HalfCheetah episode. Top: time-varying reward multiplier (gold = high-value phases). Middle: penalty weight $\lambda(t)$ --- constant for $\eta=0$ (red), adaptive for $\eta>0$ (blue). $\lambda$ decreases when budget is plentiful and spikes near depletion ($\eta=5$, late episode). Bottom: velocity traces with threshold violations (dots).}
    \label{fig:mechanism}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fig2_physical.pdf}
    \caption{When does the agent speed? (a)~Spending rate by reward cycle phase: the AC schedule ($\eta \geq 2$) sustains a uniformly high spending rate ($\sim$25\%), while the constant penalty ($\eta=0$) spends only $\sim$10\%. (b)~Reward multiplier distribution at speeding timesteps. (c)~Cumulative budget consumption: $\eta=0$ uses only half the budget; AC uses 96--99\%.}
    \label{fig:physical}
\end{figure}

\subsection{Budget Satisfaction and Reward Retention}

Table~\ref{tab:main_results} summarizes the main results, averaged over 5 training seeds (HalfCheetah) and 3 training seeds (wind farm) with 10 evaluation episodes per seed.
In HalfCheetah, the AC schedule ($\eta=2$) retains $88.9\% \pm 2.3\%$ of unconstrained reward while using 97\% of the available velocity budget, compared to only $84.3\% \pm 3.4\%$ reward and 63\% budget utilization for the constant penalty ($\eta=0$).
In the wind farm domain, the unconstrained agent uses negative yaw on $116 \pm 38$ out of 200 steps for the upstream turbine---the optimal policy relies heavily on wake steering.
With the AC schedule, negative yaw drops to within the budget while maintaining power close to the unconstrained level.

\begin{table}[t]
\centering
\caption{Main results across both domains (mean $\pm$ std over training seeds). Budget utilization reports the fraction of allowed budget actually spent. Higher utilization with high reward indicates intelligent allocation.}
\label{tab:main_results}
\begin{tabular}{lccc}
\toprule
\textbf{HalfCheetah (5 seeds)} & Reward & \% Unconstrained & Budget Used \\
\midrule
Unconstrained & $4229 \pm 468$ & 100\% & --- \\
Constant ($\eta=0$) & $3556 \pm 358$ & $84.3 \pm 3.4$\% & 63\% \\
AC ($\eta=2$) & $3759 \pm 440$ & $88.9 \pm 2.3$\% & 97\% \\
AC only (no guard) & $3752 \pm 434$ & $88.7 \pm 2.6$\% & 96\% \\
\midrule
\textbf{Wind Farm, 3-turb (10 seeds)} & Power (MW) & NegYaw (T0) & NegYaw (T1) \\
\midrule
Unconstrained & $24.9 \pm 0.8$ & $122 \pm 40$ & --- \\
Hard clip (no neg yaw) & $25.5 \pm 1.1$ & 0 & 0 \\
AC ($\eta=2$, $k=2$, gs=0.05) & $25.3 \pm 1.0$ & $10 \pm 3$ & $10 \pm 3$ \\
Constant ($\eta=0$, gs=0.1) & $24.5 \pm 1.8$ & $76 \pm 25$ & $68 \pm 30$ \\
\midrule
\textbf{Wind Farm, 9-turb ($3\!\times\!3$)} & Power (MW) & \multicolumn{2}{c}{Avg NegYaw/turb} \\
\midrule
Unconstrained & 79.2 & \multicolumn{2}{c}{$111 \pm 1$} \\
Hard clip & 75.1 & \multicolumn{2}{c}{0} \\
AC ($\eta=2$, $k=3$, gs=0.1) & 79.2 & \multicolumn{2}{c}{$\sim$95} \\
\bottomrule
\end{tabular}
\end{table}

The key comparison in Figure~\ref{fig:pareto} shows the structural advantage of adaptive scheduling.
Constant-penalty configurations face a dilemma: low guidance scale under-constrains (budget exceeded) while high guidance scale over-constrains (budget wasted, reward lost).
The AC schedule resolves this by adapting the penalty to the budget state, simultaneously achieving high utilization and high reward.

\textbf{Wind farm spatial allocation.}
Figure~\ref{fig:wf_spatial}(a) reveals that the unconstrained agent has learned wake physics: it preferentially applies negative yaw to the upstream turbine (T0, 122 steps) to deflect its wake, benefiting downstream turbines.
Under the AC budget constraint ($B=15$), all three turbines are constrained to $\sim$10 steps each, while the constant penalty ($\eta=0$) over-spends at $\sim$50 steps per turbine.
Figure~\ref{fig:wf_budget}(a) shows how the agent allocates negative yaw across budget levels.
As the budget tightens, the downstream turbine (T2) reduces negative yaw first, preserving the more valuable upstream steering.
Power retention (Figure~\ref{fig:wf_budget}b) is high across all budget levels---the median constrained power is within 5\% of unconstrained, with large IQR bands reflecting the stochastic wind conditions (variable speed and direction across episodes).
The near-100\% retention indicates that the budget constraint has minimal power cost at these budget levels, consistent with the wind farm result that negative yaw provides marginal benefit at oblique wind directions.

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_windfarm_spatial.pdf}
    \caption{Wind farm spatial yaw allocation. (a) Unconstrained agent uses negative yaw heavily on all turbines; AC ($\eta=2$) constrains to within the 15-step budget; constant penalty over-spends. (b) Turbine layout: T0 (upstream) deflects its wake to benefit T1 and T2.}
    \label{fig:wf_spatial}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_windfarm_budget.pdf}
    \caption{Wind farm budget effect. (a) Per-turbine allocation: as budget tightens, downstream T2 reduces negative yaw first, preserving upstream wake steering. (b) Power retention (median $\pm$ IQR across configurations): constrained power remains within 5\% of unconstrained, indicating near-zero power cost of budget compliance.}
    \label{fig:wf_budget}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_windfarm_timeseries.pdf}
    \caption{Wind farm yaw time series under different budget strategies (one episode, budget$=15$). Top two panels: per-turbine yaw angles---unconstrained (gray) swings $\pm$20$^\circ$, while budget-constrained methods stay near 0--5$^\circ$. Third panel: $\lambda(t)$ on log scale---constant $\eta=0$ (red) stays flat; AC $\eta=5$ (dashed) drops during surplus and spikes to $10^4$ near budget depletion. Bottom: cumulative negative yaw---AC $\eta=5$ uses the full budget by episode end; $\eta=2$ conserves heavily.}
    \label{fig:wf_timeseries}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_windfarm_timeseries_heatmap.pdf}
    \caption{Yaw angle heatmaps (turbine $\times$ timestep) showing the spatial-temporal pattern. Unconstrained (left): persistent positive/negative yaw steering across all turbines. AC $\eta=2$ (middle): nearly zero yaw---very conservative. AC $\eta=5$ (right): mostly zero with a concentrated burst of negative yaw (blue) near the end, representing strategic budget spending.}
    \label{fig:wf_heatmap}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fig3_pareto.pdf}
    \caption{Budget utilization vs.\ reward retention.
    Constant penalty (squares, red) cannot simultaneously achieve high utilization and high reward.
    AC schedule (circles, blue shades by $\eta$) clusters in the efficient top-right region.}
    \label{fig:pareto}
\end{figure}

\subsection{Hard Guard Ablation}

A central question: does the AC schedule itself prevent budget violations, or is the hard guard ($C^i < B^i$) necessary?
Table~\ref{tab:hard_guard} shows the answer on HalfCheetah: the AC schedule alone provides virtually identical budget management to AC + hard guard (244/250 violations in both cases).
The guard-only baseline ($\eta=0$, constant penalty) uses only 54\% of the budget---it constrains but does not allocate.

\begin{table}[t]
\centering
\caption{Hard guard ablation (HalfCheetah, budget=250/1000 steps, 5 seeds). The AC schedule alone (no hard guard) provides the same budget management as the full system.}
\label{tab:hard_guard}
\begin{tabular}{lccc}
\toprule
Configuration & \% Unconstrained & Budget Used & Reward \\
\midrule
Both (AC + guard) & $88.9 \pm 2.3$\% & $96.8 \pm 1.9$\% & $3759 \pm 440$ \\
AC only (no guard) & $88.7 \pm 2.6$\% & $96.4 \pm 2.0$\% & $3752 \pm 434$ \\
Guard only ($\eta=0$) & $84.3 \pm 3.4$\% & $63.0 \pm 18.8$\% & $3556 \pm 358$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Schedule Comparison: $w^*(u)=1/u$ vs.\ $w(u)=\exp(\eta(1/u - 1))$}

Figure~\ref{fig:ablations}(b) compares the theoretically optimal $u^{-\eta}$ schedule against our practical $\exp(\eta(1/u-1))$ approximation.
Both achieve similar reward retention across all $\eta$ values.
The optimal schedule is marginally better at $\eta=2$ (87.0\% vs.\ 86.6\%), confirming that the practical form is a faithful approximation.
The difference is smaller than the inter-episode variance, supporting our use of the more numerically stable exponential form.

\subsection{Budget Flexibility}

Figure~\ref{fig:flexibility} demonstrates the ``train once, constrain at deployment'' property.
A single HalfCheetah policy, trained unconstrained, is evaluated at five budget levels from 10\% to 75\% of episode length.
The reward curve is concave and bows above the linear baseline: at 25\% budget, the agent retains 86.5\% of unconstrained reward (not 25\%).
This disproportionate value capture confirms that the AC schedule concentrates spending into high-value timesteps.

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fig4_flexibility.pdf}
    \caption{Budget flexibility. Left: same policy at different budget levels (concave curve = intelligent allocation). Right: cumulative spending trajectories depart from the TWAP diagonal, spending conservatively early and catching up during high-value phases.}
    \label{fig:flexibility}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fig5_ablations.pdf}
    \caption{Ablations on HalfCheetah. (a)~Risk aversion sweep: $\eta=0$ wastes budget (53\% used), $\eta \geq 1$ achieves $>$96\% utilization and 7\% more reward. (b)~Schedule comparison: $u^{-\eta}$ (optimal) and $\exp(\eta(1/u-1))$ (practical) perform comparably. (c)~Hard guard ablation: the AC schedule alone matches the full system.}
    \label{fig:ablations}
\end{figure}

\subsection{Nonlinear Fatigue Surrogate: Why the Cost Function Matters}

Table~\ref{tab:nonlinear} compares a binary cost indicator ($\mathbf{1}[\text{yaw} < 0]$) against the nonlinear fatigue surrogate from Eq.~\ref{eq:nonlinear_cost} on the wind farm.
The binary indicator treats all negative yaw equally---but real structural fatigue depends on both the yaw magnitude and wind speed.
The results are striking:

\begin{table}[t]
\centering
\caption{Binary vs.\ nonlinear fatigue cost on 3-turbine wind farm (budget=15 steps). Cumulative fatigue computed as $\sum |y/\bar{y}|^2 \cdot (v/v_{\mathrm{ref}})^3$. The binary indicator minimizes neg-yaw \emph{count} but produces high fatigue; the nonlinear surrogate minimizes actual fatigue.}
\label{tab:nonlinear}
\begin{tabular}{lccc}
\toprule
Method & Power (MW) & NegYaw & Cum.\ Fatigue \\
\midrule
Unconstrained & 29.7 & [66, 75, 71] & 20.3 \\
\midrule
Binary $\eta=0$ (constant) & 25.8 & [18, 21, 25] & 87.6 \\
Binary $\eta=2$ (AC) & 28.1 & [8, 8, 10] & 86.2 \\
\midrule
Nonlinear $\eta=0$ (constant) & 25.9 & [28, 51, 52] & 29.8 \\
Nonlinear $\eta=2$ (AC) & 25.8 & [29, 55, 54] & 50.5 \\
Nonlinear $\eta=5$ (AC) & \textbf{29.2} & [80, 81, 83] & \textbf{12.8} \\
\bottomrule
\end{tabular}
\end{table}

The binary AC ($\eta=2$) uses only 8--10 neg-yaw steps but accumulates fatigue of 86.2---\emph{worse} than unconstrained (20.3).
This occurs because the binary constraint forces the agent to avoid negative yaw entirely, pushing it toward large \emph{positive} yaw angles that the fatigue model also penalizes.
In contrast, the nonlinear AC ($\eta=5$) uses \emph{more} neg-yaw steps (80--83) but achieves fatigue of only 12.8---a 6.8$\times$ reduction---while retaining 98\% of unconstrained power.
The nonlinear surrogate permits negative yaw freely at low wind speeds (where fatigue is cheap) and restricts it only at high wind speeds (where fatigue is expensive).
This physically meaningful allocation is impossible with a binary indicator.

\subsection{Comparison to Baselines}

We compare against several adaptive penalty strategies (Table~\ref{tab:saute}).

\textbf{Saute RL}~\citep{sootla2022saute} augments the state with remaining budget and retrains.
We test both the original soft-constraint version (which penalizes but does not guarantee satisfaction) and a \emph{Saute + clip} variant with a hard guard that forces safe actions when budget is exhausted.
Saute + clip achieves 4551 reward while satisfying the budget (233/250)---2.6\% higher than AC (4436).
However, Saute requires full retraining for each budget level.

\textbf{PID Lagrangian}~\citep{stooke2020responsive} adapts the dual variable via proportional-integral-derivative control.
We sweep 45 gain configurations ($K_p \in \{0.5, 1, 2, 5, 10\}$, $K_i \in \{0.1, 0.5, 1\}$, $K_d \in \{0.01, 0.1, 0.5\}$) and report the best.
Even with oracle-tuned gains, the best budget-satisfying PID achieves only 3900 reward---12\% below AC.
PID is fundamentally reactive: it corrects after overspending rather than anticipating depletion.

\textbf{Matched constant penalty.}
A bisection search finds the constant $\lambda=0.85$ that matches AC's 97\% utilization.
This achieves 4407 reward---comparable to AC (4436) but requiring tuning against the specific dataset.
AC achieves similar performance with a single parameter $\eta$ that generalizes across budget levels.

The decisive advantage is flexibility: AC handles budget=10\% and budget=50\% with the same policy and zero additional training.
Saute + clip requires full retraining, PID requires gain tuning, and the matched constant requires per-dataset calibration.

\textbf{Attribution: utilization vs.\ timing.}
To disentangle the contributions of budget utilization and intelligent timing, we train a separate HalfCheetah policy with constant reward (amplitude$=0$, no time-varying multiplier).
Without any timing structure to exploit, the AC schedule still achieves $86.8\%$ of unconstrained reward vs.\ $81.5\%$ for the constant penalty---a 5.3pp advantage from utilization alone.
With the time-varying multiplier (main experiments), the total advantage is 4.6pp.
This confirms the primary mechanism is fuller budget utilization, not timing.

\begin{table}[t]
\centering
\caption{Comparison of penalty scheduling strategies on HalfCheetah (budget=25\%). The AC schedule satisfies the budget while achieving reward competitive with methods that violate it. Bottom rows show AC generalizing to unseen budgets with zero retraining.}
\label{tab:saute}
\begin{tabular}{lcccl}
\toprule
Method & Reward & Violations & Used & Notes \\
\midrule
\multicolumn{5}{l}{\textit{Budget = 25\% (250/1000 steps)}} \\
Unconstrained & 5165 & --- & --- & Upper bound \\
Saute RL (soft) & 4659 & 281/250 \textcolor{red}{$\times$} & 113\% & Retrained \\
Saute + clip (hard) & 4551 & 233/250 \checkmark & 93\% & Retrained \\
Best PID ($K_p\!=\!10$) & 4176 & 253/250 \textcolor{red}{$\times$} & 101\% & Post-hoc (45 gains) \\
Matched const ($\lambda\!=\!0.85$) & 4407 & 241/250 \checkmark & 96\% & Post-hoc (tuned) \\
AC ($\eta=2$) & 4436 & 245/250 \checkmark & 98\% & Post-hoc \\
AC ($\eta=5$) & 4436 & 247/250 \checkmark & 99\% & Post-hoc \\
\midrule
\multicolumn{5}{l}{\textit{AC at unseen budgets (zero retraining)}} \\
AC ($\eta=2$, 10\%) & 4151 & 98/100 \checkmark & 98\% & \\
AC ($\eta=5$, 50\%) & 5162 & 442/500 \checkmark & 88\% & \\
\bottomrule
\end{tabular}
\end{table}

%----------------------------------------------------------------------
\section{Related Work}
\label{sec:related}
%----------------------------------------------------------------------

\textbf{Constrained MDPs.}
The Lagrangian approach to CMDPs~\citep{altman1999constrained} learns a dual variable $\mu$ via primal-dual optimization.
PID-based methods~\citep{stooke2020responsive} improve stability.
Saute RL~\citep{sootla2022saute} augments the state with remaining budget but requires retraining.
Predictive Lagrangian Optimization~\citep{chen2025predictive} forecasts future multipliers but integrates with training.
CAPS~\citep{yao2024caps} switches between pre-trained policies for different constraint levels.
Budget-Conditioned Reachability~\citep{liu2026bcr} conditions on remaining budget via reachability analysis for offline safe RL.
TREBI~\citep{lin2023trebi} uses diffusion models with real-time budget constraints that can change at deployment, the closest prior work to our setting---but it requires an offline dataset and diffusion-specific training, while our method works with any pre-trained energy-based policy.
All of these methods require training or pre-training for each constraint class; our method requires none.

\textbf{Post-hoc constraint composition.}
Composable Energy Policies~\citep{urain2023composable} compose energy functions for robotic manipulation.
Constrained Diffusers~\citep{zhang2025constrained} add safety constraints via classifier guidance.
SafeDiffuser~\citep{xiao2023safediffuser} uses control barrier functions.
FISOR~\citep{zheng2024fisor} learns a feasibility classifier for diffusion-based safe RL.
PICNN~\citep{durkin2025picnn} corrects actions via input-convex networks.
All handle \emph{instantaneous} constraints; none address cumulative budgets that span an episode.

\textbf{Budget pacing.}
Online budget allocation is well-studied in advertising~\citep{balseiro2023budget}, where PID controllers and multiplicative updates pace daily spend.
Bandits with knapsacks~\citep{agrawal2014bandits} provide regret bounds for combinatorial budget problems.
Our work transfers this structure to RL deployment via energy composition.

\textbf{Optimal execution.}
Almgren and Chriss~\citep{almgren2001optimal} derive optimal liquidation trajectories from mean-variance objectives with endogenous market impact.
Recent work combines RL with execution~\citep{ning2024execution}.
Our work is inspired by the \emph{pacing logic} of optimal execution---the urgency ratio and TWAP baseline---but the analogy has clear limits: market impact (your trading affects future prices) has no analog in our setting, and the specific sinh trajectory does not transfer.
We adopt the conceptual framework (remaining inventory vs.\ remaining time) without claiming structural equivalence.

%----------------------------------------------------------------------
\section{Discussion: Competitive Landscape and Future Directions}
\label{sec:discussion}
%----------------------------------------------------------------------

\textbf{How close is the competition?}
An honest assessment of our results reveals that the performance gap between the AC schedule and simpler alternatives is modest in some settings.
In HalfCheetah, a bisection-tuned constant penalty ($\lambda=0.85$) achieves 4407 reward at 96\% utilization---within 0.7\% of the AC schedule's 4436.
Saute RL with a hard clip retains 93\% of the budget and achieves 2.6\% higher reward than AC, at the cost of full retraining.
These results suggest that the AC schedule's advantage is not raw performance but \emph{zero-shot adaptability}: the same $\eta$ parameter works across budget levels, domains, and deployment scenarios without per-task calibration.

The matched constant baseline, while competitive at a single operating point, required 15 bisection steps (each evaluating 5 episodes) to find $\lambda=0.85$.
Changing the budget from 25\% to 10\% would require repeating this search.
The AC schedule handles this change by adjusting $u_t$ automatically---no search, no retraining, no additional evaluation budget.

\textbf{When does the AC schedule win decisively?}
The advantage is largest when: (1)~the budget is tight relative to unconstrained behavior (forcing genuine allocation decisions), (2)~conditions vary within the episode (creating high-value and low-value windows), and (3)~the policy architecture supports energy composition (enabling smooth gradient-based modulation rather than action scaling).
In our wind farm experiments, where all three conditions hold, the AC schedule concentrates negative yaw into wake-aligned wind conditions and achieves near-unconstrained power.
In HalfCheetah with constant reward (no temporal variation), the advantage narrows to pure utilization (5.3pp over constant penalty).

\textbf{Paths to widening the gap.}
Several extensions could amplify the AC schedule's advantage over simpler alternatives:

\emph{Learned risk aversion.}
Currently $\eta$ is fixed for the entire episode.
A meta-learned $\eta(s_t)$ that adapts to the current state---high when the agent recognizes favorable conditions, low otherwise---could concentrate budget spending more precisely than the urgency ratio alone.
This would require a small auxiliary network trained offline on logged episodes.

\emph{Benefit prediction.}
The AC schedule reacts to budget state but does not predict future opportunities.
A lightweight forecasting model $\hat{b}(t+k \mid s_t)$ that estimates upcoming benefit (e.g., expected wind direction or upcoming terrain) could inform a lookahead urgency calculation, shifting from reactive to proactive budget management.

\emph{Multi-agent budget sharing.}
In the wind farm setting, turbines currently have independent budgets.
A shared farm-level budget with per-turbine allocation---analogous to portfolio optimization across assets---could exploit cross-turbine complementarities (e.g., spending T0's budget when T2 has surplus).

\emph{Tighter integration with policy training.}
The post-hoc paradigm trades optimality for flexibility.
A hybrid approach that \emph{trains} with a generic budget-awareness module (not tied to a specific budget level) and \emph{deploys} with the AC schedule for budget-specific adaptation could close the gap to retrained methods while preserving zero-shot generalization.

%----------------------------------------------------------------------
\section{Conclusion}
\label{sec:conclusion}
%----------------------------------------------------------------------

We introduced an Almgren-Chriss-inspired penalty schedule for enforcing cumulative budget constraints on pre-trained RL policies at deployment time.
The schedule adapts penalty strength based on the urgency ratio---remaining budget relative to remaining time---and composes post-hoc with energy-based policies via per-agent energy addition.
Across three domains (wind farm yaw control, MuJoCo locomotion, and Safety Gymnasium navigation), the method satisfies cumulative budgets while retaining 85--99\% of unconstrained reward, outperforming constant penalties in budget utilization (93--100\% vs.\ 47--63\%) and matching retrained baselines to within 3\%.

The method's primary value is practical: a single pre-trained policy serves arbitrary budget levels, per-agent heterogeneous constraints, and instant specification changes---all without retraining.
The performance gap to optimally-tuned alternatives is small but the deployment cost gap is large.
As RL systems move from research benchmarks to industrial applications where constraints emerge, evolve, and differ across installations, the ability to compose constraints post-hoc becomes increasingly important.

\textbf{Limitations.}
The Boltzmann response assumption (Proposition~\ref{prop:optimal}) is a motivating analysis, not an optimality guarantee.
The method works best when the base penalty scale is calibrated relative to the learned energy head.
The advantage over simpler alternatives is modest when conditions do not vary within episodes.
Finite-sample constraint satisfaction relies on the exponential wall backstop rather than the smooth schedule alone.

\textbf{Reproducibility.}
Code, trained checkpoints, and experiment scripts are available at \url{[anonymized]}.

%----------------------------------------------------------------------
% References
%----------------------------------------------------------------------

\bibliography{references}
\bibliographystyle{plainnat}

\end{document}
